using UnityEngine;

public class PlayerAudio : MonoBehaviour
{
    /*
    [Header("References")]
    [SerializeField] private PlayerController player;
    [SerializeField] private AudioSource audioSource;

    [Header("Audio Clips")]
    [Tooltip("Element 0 = Normal Floor, Element 1 = Gravel")]
    [SerializeField] private AudioClip[] footstepClips;

    [Header("Settings")]
    [SerializeField] private float walkInterval = 0.5f;   // Time between steps when walking
    [SerializeField] private float sprintInterval = 0.3f; // Time between steps when sprinting
    [SerializeField] private float velocityThreshold = 0.1f; // Min speed to play sound

    private float stepTimer;
    */
}